cd /path/to/SMART_CLASSROOM

mkdir voice_service
cd voice_service
mkdir config models routes sockets middleware client tests docs


npm init + install all dependencies
cd voice_service

npm init -y


npm install express socket.io mongoose dotenv jsonwebtoken cors

npm install --save-dev nodemon jest supertest socket.io-client


package.json: "scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js",
  "test": "jest --watchAll=false"
},
"jest": {
  "testEnvironment": "node"
}

